<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:elevation="8dp"
    android:background="@drawable/bg_dialog_winet"
    android:gravity="center_vertical">

    <!-- Título -->
    <TextView
        android:id="@+id/dialogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:fontFamily="@font/dancing_script"
        android:text="Botellas en su punto óptimo de consumo🍷"
        android:textColor="#B22034"
        android:textSize="26sp"
        android:textStyle="bold" />

    <!-- Texto descriptivo -->
    <TextView
        android:id="@+id/dialogDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#444444"
        android:textSize="15sp"
        android:layout_marginBottom="12dp" />

    <!-- Lista de vinos -->
    <TextView
        android:id="@+id/dialogWineList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#222222"
        android:textSize="15sp" />

    <!-- Botones -->
    <LinearLayout
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <Button
            android:id="@+id/btnCloseDialog"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cerrar"
            android:textColor="#B22034"
            android:background="@android:color/transparent" />

        <Button
            android:id="@+id/btnOpenCellar"
            android:layout_width="wrap_content"
            android:foreground="?attr/selectableItemBackground"
            android:layout_height="wrap_content"
            android:text="Ver mi bodega"
            android:textColor="@android:color/white"
            android:backgroundTint="#B22034"
            android:layout_marginStart="16dp"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp" />
    </LinearLayout>

</LinearLayout>
